# TradeGuard

The frontend mockup has been converted to a React single-page application using
Vite and React Router.

## Run locally

```powershell
cd TradeGuard-frontend-mock
npm install
npm run dev
```

Routes: `/login`, `/register`, `/dashboard`, `/orders`, `/buy`, and `/sell`.
